#!/bin/bash

readonly INPUTFILE=$1

readonly TMPDIR=$(mktemp -d)

cat $INPUTFILE | awk '{print $1 " " $2 " " $3}' > $TMPDIR/names
cat $INPUTFILE | awk '{print $4}' | shuf > $TMPDIR/values

paste $TMPDIR/names $TMPDIR/values | column -t

rm -r $TMPDIR
